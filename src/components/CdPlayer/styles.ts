import styled from "@emotion/styled";

export const CdPlayerContainer = styled.div`
    position: relative;
`

export const CdContainer = styled.div`
    position: absolute;
    top:50%;
    left: 50%;
    transform: translate(-50%, -50%);
`

export const CdPlayer = styled.img`
    display: block;
`